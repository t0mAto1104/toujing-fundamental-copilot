# 行情数据页面验收（2026-09-04）

## 2026-09-06 扩展验收

- 按 a-stock-data 的腾讯行情、新浪因子 HTTP 方案扩展；保持现有日/周/月与分钟 K 线、备用源、D1 自选、刷新与隐私隔离逻辑。无新依赖、数据库迁移、API Key、AI 请求或后台任务。
- 新增 MA5/10/20，使用正在展示的同周期、同复权收盘价计算；第 N 根首次有值，之前显示空值。十字光标与键盘共用选中时间；轮询时 MA 同步更新。
- 腾讯快照新增买卖五档（量为手）、PE 及其口径、PB、总/流通市值（从亿元转元）。39 为动态 PE，55 有值才标 TTM，46 为 PB，不使用 43 振幅。指数盘口不适用；ETF 不捏造 PE/PB，交易市值不冒充基金净资产。
- 新增独立 `/api/quotes/search`，支持股票/ETF/七个指数，不放宽公司 AI 研究的证券限制。ETF 三位价格覆盖图表、图例、盘口、顶部、自选和快捷列表；裸同码股票/指数返回候选，不丢失交易所。REIT 不冒充 ETF。
- 新增 `/api/quotes/factors`：新浪 qfq/hfq 原始因子、有效日期、可选 s/u 项，D1 缓存一小时，展开时才请求。保留 ETF 的现金/份额调整项，不把 f=1 解读为没有分红。
- 因子仅供核验，不对腾讯/东财已复权 K 线二次复权，不混用不同来源的基准；指数不适用，北交所因子源不支持时明确返回错误，但保留其独立来源的复权 K 线能力。
- `market-quotes`、`market-watchlist`、`market-listings`、`research-data` 共 30 项测试通过；类型检查、修改文件 lint、生产构建通过。
- 本地真实 HTTP 验证成功：贵州茅台、沪深300ETF华泰柏瑞、创业板ETF易方达、上证指数快照；ETF 名称/代码搜索；000016 同时返回 *ST康佳A（SZ）/上证50（SH）；上证日 K 600 根、510300 日前复权 600 根、159915 周后复权 600 根与 5 分钟 320 根、茅台月后复权 301 根。
- 新浪真实因子：茅台 qfq/hfq 各 33 条；510300 hfq 15 条，最新记录 f=1、s=1、u=0.88。北交所因子返回明确不支持。当前周末，实际报价/末根日期为 2026-09-04，未当作 09-06 新成交。
- 未进行浏览器点击/视觉验收或真实账户自选端到端写入；D1 ETF 添加/删除及账户隔离通过自动测试。本轮仅本地更新，尚未公开发布。

## 范围

- `/quotes` 默认上证指数，提供七个沪深北大盘指数；股票可通过原有联想搜索选择。
- 日、周、月及 5/15/30/60 分钟 K 线；股票日周月支持不复权、前复权、后复权。
- Lightweight Charts 5.2.1：十字光标 OHLC/量额、成交量副图、拖动、缩放、键盘查看、全屏。
- 腾讯 HTTP 主源、东方财富历史备用源；复用现有 HTTP 超时/限流及 D1 快照。
- 报价每 15 秒、K 线每 30 秒刷新；仅当前可见页面轮询，失败冷却 60 秒。无 AI 调用或后台任务。
- 登录后 D1 保存最多 50 只自选股；服务端按认证用户隔离，忽略客户端提供的名称/用户信息。
- 数据缺失显示空值，刷新失败标记旧快照；顶部报价和 K 线分别保留其来源时间和链接。

## 已完成检查

- `npm run build`、`npx tsc --noEmit`、新增/修改文件的 oxlint 和 `git diff --check` 均通过。
- `tests/market-quotes.test.ts`、`tests/market-watchlist.test.ts`、现有 HTTP 超时及证券搜索测试共 14 项通过。
- 测试通过 esbuild 打包为 Node ESM 后，使用 `scripts/research-eval-runtime.mjs` 的离线 Cloudflare 环境适配器执行；模型未调用。
- 行情解析校验证券身份/时间、空值、OHLC、量额口径、排序去重；异常路由 400/503；缓存失效回退标记。
- 实际 SQLite 加载新迁移，验证自选持久化、账号隔离、幂等添加、错误用户删除无效、50 只限制。
- 本地 HTTP：`/quotes` 返回 200，七指数报价有效，匿名 `/api/watchlist` 返回 401。
- 实际上游数据检查：上证日 K、北证50日 K、贵州茅台周前复权/月后复权、平安银行5分钟、维琪科技日 K、创业板60分钟均返回有效数据。
- 北证50/维琪科技主源仅一根时，备用源分别返回 600/30 根，未伪造额外历史。

## 迁移说明

- `0003_market_watchlist.sql` 仅新建自选表，不修改或删除现有数据。
- 历史迁移快照落后于已有 `users.allowed_ai_models` 运行时列；新快照与现有 schema 对齐，但本次 SQL 不重复添加该旧列，旧迁移保持不变。
- 新表完全由迁移管理，接口不执行运行时 CREATE/ALTER；已验证本地 D1 应用成功，生产应用由发布流程执行。

## 验证边界

- 未执行浏览器交互/视觉验收，也未使用真实登录用户进行端到端自选写入；认证拒绝与存储隔离已分别验证。
- 公开 HTTP 行情不等于交易所授权逐笔实时行情；休市/停牌仍显示来源最后时间，页面已有说明。
- `npm audit --omit=dev` 因 npm 审计服务超时未完成；未以自动升级方式改动无关依赖。
- 当前仅本地完成，公开部署须经用户确认。
